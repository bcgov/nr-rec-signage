import React from 'react';
import SignLibrary from '@/components/SignLibrary';

const ApprovedSign: React.FC = () => {
  return <SignLibrary libraryStatus='approved' />;
};

export default ApprovedSign;
